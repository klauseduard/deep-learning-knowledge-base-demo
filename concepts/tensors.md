# Tensors

> Multi-dimensional arrays that serve as the universal data structure in deep learning, storing both the data flowing through networks and the learned parameters.

> **Note:** This is a placeholder article in the demo repository. The full content is available in the complete knowledge base. The tagline, connections, and structure are preserved to show how articles link together.

## Connections

- [[gradient-descent]] — optimization updates are applied to parameter tensors
- [[backpropagation]] — autograd traverses the computational graph of tensor operations
- [[convolutional-neural-networks]] — operate on 3D/4D tensors with spatial structure
- [[activation-functions]] — element-wise non-linear operations on tensors
- [[gpu-and-accelerator-hardware]] — Tensor Cores and memory hierarchy determine how tensor operations are executed
- [[quantization]] — reducing tensor precision (FP32 → INT8/INT4) for efficient inference
- [[large-language-models]] — quantization of weight tensors enables local inference
