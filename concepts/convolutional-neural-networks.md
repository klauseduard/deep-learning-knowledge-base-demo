# Convolutional Neural Networks

> The standard architecture for processing images and structured signals, using local filters shared across all spatial positions to exploit translation invariance and locality.

> **Note:** This is a placeholder article in the demo repository. The full content is available in the complete knowledge base. The tagline, connections, and structure are preserved to show how articles link together.

## Connections

- [[activation-functions]] — ReLU applied after each convolution layer
- [[skip-connections]] — enable very deep CNNs via residual connections
- [[regularization]] — batch normalization and channel-wise dropout used throughout CNNs
- [[multilayer-perceptrons]] — the classifier head of a CNN is typically an MLP
- [[attention-and-transformers]] — Vision Transformers (ViT) challenge CNN dominance
- [[diffusion-models]] — U-Net architecture (a CNN with skip connections) is the standard denoiser
